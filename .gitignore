# Secrets — never commit the real .env
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Playwright browser cache (installed in CI each run)
# NOTE: data/jobs.db is intentionally NOT ignored — it is the committed source of truth.

# Outreach — keep recruiter emails, drafts, and the sent-log private.
outreach/recruiters.csv
outreach/drafts/
outreach/sent_log.csv
outreach/*.sent
