aiohttp>=3.9
requests>=2.31
python-dotenv>=1.0
pyyaml>=6.0
playwright>=1.44
feedparser>=6.0
python-dateutil>=2.8
# Gemini is called via plain REST (requests) — no google SDK needed.
# sqlite3 is part of the Python standard library.
